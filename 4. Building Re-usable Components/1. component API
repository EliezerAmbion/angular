We use input and output properties for out custom components.
In order to make a component more reusable, we want to add input and output properties.

We use INPUT properties to pass input or state to a component.
We use OUTPUT properties to raise events from your custom components.

The combination of input and output components, make up for what we call the PUBLIC API of that component.

API - Application Programming Interface.